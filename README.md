# 🛌 Lazy Person's Bed Code Terminal (L.P.B.C.T)

> **CN**: 坐着写代码时间长了腰疼。为了能躺在床上或者在外面晒太阳的时候顺手写写代码，才做了这个小工具。分享给一样想偷懒的朋友们。
> **EN**: Built this because sitting at a desk for too long is a pain. Now I can code in bed or under the sun. Sharing this with anyone as lazy as I am.

---

## 🐍 运行环境 / Environment (Python 3.13.1)

**CN**: 环境是我个人开发用的，如果你的环境版本不一致导致跑不通，可能需要自己调试一下。
**EN**: This is my personal development environment. If your setup differs and causes issues, you may need to debug it yourself.

- **Python**: 3.13.1
- **核心依赖 / Core Libs**: 
  - `Flask 3.1.2`, `flask-cors 6.0.2`, `requests 2.32.3`, `PyYAML 6.0.3`
- **安装指令 / Install**: 
  `pip install flask flask-cors requests pyyaml`

---

## 📂 目录结构与配置 / Structure & Config

### 1. 服务端 / Server Side
- **app.py**: 服务端主程序 / Server program
- **static/**: 静态资源目录 (index.html, manifest.json, sw.js) / Static assets
- **command/**: 指令存放目录 (**需手动创建**) / Task folder (**Manual creation required**)
- **result/**: 结果存放目录 (**需手动创建**) / Result folder (**Manual creation required**)
- **Config**: 必须在 `app.py` 中设置 `API_TOKEN` / Must set `API_TOKEN` in `app.py`.

### 2. 客户端 / Client Side
- **executor.py**: 客户端执行程序 / Client executor
- **BASE_DIR**: 本地源码根目录 / Your local source code root
- **VENV Config**: 
  - **CN**: `VENV_PYTHON` 指向虚拟环境路径。如果你连这个都整不明白，建议直接填实际 Python 安装路径。如果你还是跑不通，可能说明你并不适合本工具；强行运行相当于给自己开了个靶机。
  - **EN**: `VENV_PYTHON` points to your venv path. If this is confusing, use the actual Python path. If you still can't get it to work, this tool might not be for you; running it incorrectly is essentially turning your own machine into a **target machine**.

---

## 🚀 使用说明 / Usage

1. **Run**: 先运行服务端 `python app.py`，再启动客户端 `python executor.py`。 / Start server then client.
2. **Access**: 浏览器打开 `http://YOUR_IP:8000/ui/`。 / Open UI in browser.
3. **Connect**: 在 **Setting** 标签页填入地址及 `API_TOKEN`。 / Configure address and token in Settings.

---

## 🗺️ 实现逻辑 / Logic Flow

1. **Trigger**: 网页端发指令，以 YAML 格式存入 `command/`。 / UI triggers task, saved as YAML.
2. **Execute**: 客户端轮询该目录，执行后删除 YAML文件。 / Client polls, executes, and deletes YAML.
3. **Return**: 结果存入 `result/` 并由前端展示后自动清理。 / Result saved to server and auto-cleaned after display.

---

## ⚠️ 几点说明 / Important Notes

- **关于本说明文档 / About this README**:
  - **CN**: 为了贯彻“懒”的精神，**本 README 全文由 AI 生成**（当然，是在我的反复调教和纠正下完成的）。
  - **EN**: To keep with the spirit of "laziness," **this README was entirely generated by AI** (under my constant guidance and correction, of course).

- **关于编辑器 / About the Editor**: 
  - **CN**: 考虑到现在大部分人都是让 AI 生成代码（直接复制粘贴），所以我没实现复杂的编辑器功能，够用就好。
  - **EN**: Since most people use AI to generate code (just copy & paste), I didn't bother implementing a complex editor. Simple and functional is the goal.

- **AI 协作建议 / AI Collaboration**: 
  - **CN**: **经验分享**：给 AI 发送短而精准的代码片段效果最好。代码太长了它容易产生**幻觉**，最后是你自己**抓狂**。
  - **EN**: **Pro Tip**: AI works best with short, precise snippets. Overloading it with long files often triggers **AI hallucinations**, which will eventually **drive you crazy**.

- **代码质量与安全 / Quality & Safety**: 
  - **CN**: 代码多为 AI 辅助生成，难免有 Bug。**执行端没有任何路径限制，可以访问电脑里的所有文件夹**。千万别在公网暴露，藏好 Token。
  - **EN**: Most of the code is AI-assisted; expect bugs. **The executor has no path restrictions and can access all folders on your computer**. DO NOT expose this to the public internet and keep your Token safe.

- **项目状态 / Status**: Beta 版本，后续大概率不会频繁维护。 / Beta version. No frequent maintenance planned.

---

祝使用愉快。 / Enjoy!
